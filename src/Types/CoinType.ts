export type Coin = {
  symbol: string;
  id: string;
  image: string;
  supply: number;
  price: number;
  description: string;
};

export type CoinCard = {
  name: string;
  id: string;
  symbol: string;
};
